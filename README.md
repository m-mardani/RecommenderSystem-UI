# RecommenderSystem-UI

رابط کاربری پیشرفته Next.js برای سیستم توصیه‌گر مبتنی بر FastAPI

## معرفی

این پروژه یک رابط کاربری کامل و حرفه‌ای برای سیستم توصیه‌گر است که با استفاده از فناوری‌های مدرن وب ساخته شده است.

## تصاویر

### صفحه ورود
![Login Page](https://github.com/user-attachments/assets/6d409f23-6751-49ef-9211-cfacbdb1dc5c)

### صفحه ثبت‌نام
![Register Page](https://github.com/user-attachments/assets/a4841657-5f76-4a00-8ea7-a0127614920d)

## ویژگی‌های کلیدی

- ✅ **Next.js 16** با TypeScript
- ✅ **TailwindCSS** برای طراحی واکنش‌گرا
- ✅ **JWT Authentication** با تازه‌سازی خودکار توکن
- ✅ **Axios** برای مدیریت درخواست‌های HTTP
- ✅ **کنترل دسترسی مبتنی بر نقش** (ADMIN/USER)
- ✅ **پشتیبانی کامل از فارسی** (RTL + فونت Vazirmatn)
- ✅ **مدیریت حالت‌های بارگذاری و خطا**
- ✅ **صفحات کامل**: ورود، ثبت‌نام، داشبورد، داده‌ها، آموزش، پایش، مدل‌ها، توصیه‌ها

## صفحات پیاده‌سازی شده

### 🔐 احراز هویت
- **صفحه ورود** - ورود کاربران با نام کاربری و رمز عبور
- **صفحه ثبت‌نام** - ثبت‌نام کاربران جدید با اعتبارسنجی فرم

### 📊 داشبورد
- نمایش آمار کلی سیستم (تعداد داده‌ها، مدل‌ها، کارهای فعال)
- دسترسی سریع به عملکردهای اصلی
- کارت‌های اطلاعاتی با آیکون‌های جذاب

### 💾 مدیریت مجموعه داده‌ها
- لیست نمایش مجموعه داده‌ها
- بارگذاری فایل CSV/JSON
- حذف مجموعه داده‌ها
- نمایش اطلاعات تعداد سطر و ستون

### 🧠 آموزش مدل
- انتخاب مجموعه داده
- انتخاب نوع مدل (فیلتر مشارکتی، محتوا، ترکیبی، و...)
- شروع فرآیند آموزش

### 📈 پایش کارهای آموزشی
- نمایش وضعیت کارها (در انتظار، در حال اجرا، تکمیل شده، ناموفق)
- نمایش پیشرفت آموزش
- بروزرسانی خودکار هر 5 ثانیه
- امکان لغو کارها

### 🎯 مدل‌های آموزش داده شده
- لیست مدل‌های موجود
- نمایش معیارهای عملکرد
- استفاده از مدل برای توصیه
- حذف مدل‌ها

### ⭐ دریافت توصیه‌ها
- ورودی شناسه کاربر
- انتخاب مدل
- تعداد توصیه‌های مورد نیاز
- نمایش نتایج در جدول

## شروع سریع

### پیش‌نیازها

- Node.js 18+
- npm یا yarn
- سرور FastAPI در حال اجرا

### نصب

```bash
cd frontend
npm install
```

### تنظیمات

فایل `.env.local` را ایجاد کنید:

```bash
cp .env.example .env.local
```

و آدرس API را تنظیم کنید:

```env
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
```

### اجرا

```bash
npm run dev
```

برنامه در آدرس [http://localhost:3000](http://localhost:3000) در دسترس خواهد بود.

## ساختار پروژه

```
frontend/
├── app/                      # صفحات Next.js
│   ├── login/               # صفحه ورود
│   ├── register/            # صفحه ثبت‌نام
│   ├── dashboard/           # داشبورد
│   ├── datasets/            # مجموعه داده‌ها
│   ├── training/            # آموزش مدل
│   ├── jobs/                # پایش کارها
│   ├── models/              # مدل‌ها
│   └── recommendations/     # توصیه‌ها
├── components/              # کامپوننت‌های React
│   ├── auth/               # کامپوننت‌های احراز هویت
│   └── common/             # کامپوننت‌های مشترک
├── contexts/               # React Context
│   └── AuthContext.tsx     # مدیریت احراز هویت
├── lib/                    # کتابخانه‌ها
│   ├── api/               # API services
│   └── utils/             # توابع کمکی
└── types/                  # TypeScript types
```

## ویژگی‌های امنیتی

### 🔒 JWT Authentication
- Access Token برای احراز هویت درخواست‌ها
- Refresh Token برای تازه‌سازی خودکار
- مدیریت خودکار توکن‌های منقضی شده

### 🛡️ محافظت از مسیرها
- تمام صفحات به جز ورود و ثبت‌نام محافظت شده‌اند
- هدایت خودکار کاربران غیرمجاز به صفحه ورود

### 👥 کنترل دسترسی مبتنی بر نقش
- تفکیک کاربران ADMIN و USER
- محدودیت دسترسی بر اساس نقش کاربر

## فناوری‌های استفاده شده

- **Next.js 16** - فریمورک React با App Router
- **TypeScript** - توسعه با تایپ استاتیک
- **TailwindCSS** - استایل‌دهی مدرن
- **Axios** - کلاینت HTTP
- **jwt-decode** - رمزگشایی و مدیریت JWT

## مستندات

- 📖 [راهنمای استفاده](./USAGE_GUIDE.md)
- 🚀 [راهنمای استقرار](./DEPLOYMENT.md)
- 📚 [مستندات فرانت‌اند](./frontend/README.md)

## API Endpoints مورد نیاز

بک‌اند FastAPI باید endpoint های زیر را فراهم کند:

**احراز هویت:**
- `POST /api/auth/login` - ورود
- `POST /api/auth/register` - ثبت‌نام
- `POST /api/auth/refresh` - تازه‌سازی توکن
- `GET /api/auth/me` - اطلاعات کاربر

**مجموعه داده‌ها:**
- `GET /api/datasets` - لیست داده‌ها
- `POST /api/datasets` - بارگذاری
- `DELETE /api/datasets/{id}` - حذف

**آموزش:**
- `POST /api/training/start` - شروع آموزش
- `GET /api/training/jobs` - لیست کارها

**مدل‌ها:**
- `GET /api/models` - لیست مدل‌ها
- `DELETE /api/models/{id}` - حذف

**توصیه‌ها:**
- `POST /api/recommendations` - دریافت توصیه

## توسعه

### Build برای Production

```bash
npm run build
```

### اجرای Production

```bash
npm start
```

### Lint

```bash
npm run lint
```

## مشارکت

برای گزارش باگ یا درخواست ویژگی جدید، لطفاً Issue ایجاد کنید.

## لایسنس

MIT